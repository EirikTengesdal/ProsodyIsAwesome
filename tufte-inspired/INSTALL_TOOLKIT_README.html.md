---
title: "Wavelet Prosody Toolkit"
subtitle: "Installation Guide"
# documenttype: Handout
abstract: |
  This guide provides **cross-platform installation instructions** for the Wavelet Prosody Toolkit on Windows, macOS, and Linux. Two installation methods are provided: a global user-level installation for general use across multiple projects, and a per-project installation with isolated dependencies for reproducible research environments. The guide is based on suggestions generated by GitHub Copilot.

author:
  - name: Eirik Tengesdal
    email: "eirik.tengesdal@iln.uio.no; eirik.tengesdal@oslomet.no"
    orcid: 0000-0003-0599-8925
    url:
      - "https://www.oslomet.no/en/about/employee/eiten9710/"
      #- "https://www.hf.uio.no/iln/english/people/aca/scandinavian-languages/temporary/eirikten/"
    # corresponding: true
    affiliation:
      - name: Oslo Metropolitan University
        department: Department of Early Childhood Education
        url: "https://www.oslomet.no/en/about/lui/blu/"
        id: 1
      - name: University of Oslo
        department: Department of Linguistics and Scandinavian Studies
        url: "https://www.hf.uio.no/iln/english/"
        id: 2
toc: true
toc-depth: 2
date: last-modified
date-format: iso
execute:
  keep-md: true
  keep-typ: true
bibliography: []
format:
  html:
    lang: en-UK
    theme:
      light: flatly
      dark: darkly
    # comments:
    #   hypothesis: true
    fontsize: 1.1rem
    mainfont: Minion 3 #Minion 3, FandolKai
    sansfont: Myriad Pro
    include-in-header:
      - text: |
          <style>
          body {
            font-variant-numeric: oldstyle-nums;
          }

          .sourceCode, pre {
            font-variant-numeric: normal;
          }
          </style>
    default-image-extension: svg
  tufte-inspired-typst: default
---

# Introduction

The [Wavelet Prosody Toolkit](https://github.com/asuni/wavelet_prosody_toolkit) is a powerful tool for analysing prosodic features in speech. This guide helps you install it on Windows, macOS, or Linux, but no guarantees are made.

## Support
For issues specific to this installation guide, please email Eirik Tengesdal at [eirik.tengesdal@oslomet.no](mailto:eirik.tengesdal@oslomet.no).

<!--
::: {.column-margin}
**Quick Links**

- [GitHub Repository](https://github.com/asuni/wavelet_prosody_toolkit)
- Global Installation (recommended)
- Per-Project Installation
:::
 -->

# Which Installation Method?

## Global Installation
<!-- {.unnumbered} -->

This is the recommended method for general use. Choose this if you:

- Want to use the toolkit across multiple projects
- Prefer simple access from anywhere
- Do not need different toolkit versions for different projects

## Per-Project Installation
<!-- {.unnumbered} -->

Choose this if you:

- Want isolated dependencies per project
- Need different toolkit versions for different projects
- Prefer strict reproducibility

::: {.aside}
**Recommendation**

For most users, the **global installation** is simpler and more convenient. Use per-project only when you need strict dependency isolation.
:::

# Option 1: Global User-Level Installation

This installs the toolkit once to your user Python environment, making it available from any directory.

## Installation

::: {.column-margin}
**Default Locations**

- Windows: `%USERPROFILE%\wavelet_prosody_toolkit`
- macOS/Linux: `~/wavelet_prosody_toolkit`
:::

### Windows (PowerShell)

::: {.wideblock when-format="typst"}
```powershell
# Navigate to where you downloaded the script
cd path\to\scripts

# Install to default location
.\install_toolkit_standalone.ps1

# OR install to custom location
.\install_toolkit_standalone.ps1 -InstallDir "C:\Tools\wavelet_prosody_toolkit"
```
:::

### macOS/Linux (Bash)

::: {.wideblock when-format="typst"}
```bash
# Make script executable
chmod +x install_toolkit_standalone.sh

# Install to default location
./install_toolkit_standalone.sh

# OR install to custom location
./install_toolkit_standalone.sh /path/to/your/preferred/location
```
:::

## Usage

After installation, you can use the toolkit from **any directory**:

### Windows

::: {.column-page-right}
```powershell
# Launch the GUI
python -m wavelet_prosody_toolkit.wavelet_gui

# Or use in your Python scripts
python your_analysis_script.py
```
:::

### macOS/Linux

::: {.column-page-right}
```bash
# Launch the GUI
python3 -m wavelet_prosody_toolkit.wavelet_gui

# Or use in your Python scripts
python3 your_analysis_script.py
```
:::

::: {.column-margin}
**Python Usage**

In your Python code (all platforms):

```python
import wavelet_prosody_toolkit
# Use toolkit functions
```
:::

## Updating the Toolkit

### Windows

::: {.column-page-right}
```powershell
cd $HOME\wavelet_prosody_toolkit
git pull
```
:::

### macOS/Linux

::: {.column-page-right}
```bash
cd ~/wavelet_prosody_toolkit
git pull
```
:::

# Option 2: Per-Project Installation

This creates a project-specific installation with its own virtual environment.

## Installation

### Windows (PowerShell)

::: {.column-page-right}
```powershell
# In your project directory
.\install_toolkit_project.ps1
```
:::

### macOS/Linux (Bash)

::: {.column-page-right}
```bash
# In your project directory
chmod +x install_toolkit_project.sh
./install_toolkit_project.sh
```
:::

## Usage

::: {.column-margin}
**Virtual Environment**

The toolkit is installed in an isolated environment (`.venv`) within your project directory.
:::

### Windows

::: {.column-page-right}
```powershell
# Activate the virtual environment
.\.venv\Scripts\Activate.ps1

# Launch the GUI
python -m wavelet_prosody_toolkit.wavelet_gui

# Run your analysis scripts
python your_analysis_script.py
```
:::

### macOS/Linux

::: {.column-page-right}
```bash
# Activate the virtual environment
source .venv/bin/activate

# Launch the GUI
python -m wavelet_prosody_toolkit.wavelet_gui

# Run your analysis scripts
python your_analysis_script.py
```
:::

## Updating the Toolkit

### Windows

::: {.column-page-right}
```powershell
cd vendor\wavelet_prosody_toolkit
git pull
```
:::

### macOS/Linux

::: {.column-page-right}
```bash
cd vendor/wavelet_prosody_toolkit
git pull
```
:::

# Prerequisites

Before running either script, ensure you have the following installed:

## All Platforms

::: {.column-margin}
**Python Version**

Python 3.8 or later is recommended. The toolkit may work with earlier versions but has not been tested.
:::

### Python 3

**Windows:**
```powershell
python --version
```
Download from: [python.org/downloads](https://www.python.org/downloads/)

**macOS/Linux:**
```bash
python3 --version
```

### Git

**Windows:**
```powershell
git --version
```
Download from: [git-scm.com/download/win](https://git-scm.com/download/win)

**macOS/Linux:**
```bash
git --version
```

## Platform-Specific

### macOS Only

Xcode Command Line Tools:

```bash
xcode-select --install
```

### Windows Only

Microsoft C++ Build Tools (if PyQt6 fails to install):

- Download [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/)
- Select "Desktop development with C++"

::: {.column-margin}
**Dependencies**

The following packages will be automatically installed:

- PyWavelets
- scipy
- numpy
- matplotlib
- PyQt6
:::

# Quick Start Example

## Global Installation

### Windows

```powershell
# Install once
.\install_toolkit_standalone.ps1

# Navigate to your project
cd C:\Users\YourName\my_prosody_analysis

# Create your analysis script
@"
import wavelet_prosody_toolkit as wpt
# Your analysis code here
"@ | Out-File -Encoding utf8 analyze.py

# Run it
python analyze.py
```

### macOS/Linux

```bash
# Install once
./install_toolkit_standalone.sh

# Navigate to your project
cd ~/my_prosody_analysis

# Create your analysis script
cat > analyze.py << 'EOF'
import wavelet_prosody_toolkit as wpt
# Your analysis code here
EOF

# Run it
python3 analyze.py
```

## Per-Project Installation

### Windows

```powershell
# In your project directory
.\install_toolkit_project.ps1

# Activate environment
.\.venv\Scripts\Activate.ps1

# Create and run your analysis
python analyze.py
```

### macOS/Linux

```bash
# In your project directory
./install_toolkit_project.sh

# Activate environment
source .venv/bin/activate

# Create and run your analysis
python analyze.py
```

# Troubleshooting

## Windows Issues

### PowerShell execution policy error

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

::: {.column-margin}
**Common Issues**

Most installation problems stem from missing prerequisites or incorrect Python/Git installation.
:::

### "python: command not found"

- Ensure Python is in your PATH
- Try using `py` instead of `python`
- Reinstall Python and check "Add Python to PATH"

### PyQt6 installation fails

Install Microsoft C++ Build Tools:

- Download from: [visualstudio.microsoft.com](https://visualstudio.microsoft.com/downloads/)
- Select "Desktop development with C++"

## macOS/Linux Issues

### "Permission denied" when running scripts

```bash
chmod +x install_toolkit_standalone.sh
chmod +x install_toolkit_project.sh
```

### Python command not found

Replace `python3` with `python` if your system uses that command.

### PyQt6 installation fails on macOS

You may need to install additional system libraries:

```bash
brew install python-tk
```

## All Platforms

### Git clone fails

Ensure you have internet connection and Git is installed:

**Windows:**
```powershell
git --version
```

**macOS/Linux:**
```bash
git --version
```

### Import errors after installation

Ensure you're using the correct Python environment:

- For global install: Use system Python
- For project install: Activate the virtual environment first

# Summary of Installation Files

The following table summarises the installation scripts included in this package:

::: fullwidth
| Purpose | Windows | macOS/Linux |
|---------|---------|-------------|
| Global installation | `install_toolkit_standalone.ps1` | `install_toolkit_standalone.sh` |
| Per-project installation | `install_toolkit_project.ps1` | `install_toolkit_project.sh` |
:::


::: {.column-margin}
**File Distribution**

All files are included in the distribution package and can be used independently.
:::

# Recommended Approach

**For most users**: Use the **global installation**:

- Windows: `install_toolkit_standalone.ps1`
- macOS/Linux: `install_toolkit_standalone.sh`

This is simpler and allows you to use the toolkit across all your projects without reinstalling.

**For advanced users**: Use **per-project installation**:

- Windows: `install_toolkit_project.ps1`
- macOS/Linux: `install_toolkit_project.sh`

Use this when you need strict dependency management or are collaborating with others who need reproducible environments.

# Additional Resources

::: {.column-margin}
**Support**

For issues specific to the toolkit itself (not installation), please refer to the GitHub repository's issue tracker.
:::

- **Toolkit Repository**: [github.com/asuni/wavelet_prosody_toolkit](https://github.com/asuni/wavelet_prosody_toolkit)
- **Installation Method**: pip install in editable mode (allows toolkit updates via `git pull`)
- **Installation Type**: User-level (no admin/sudo required)

# Acknowledgements {.unlisted}

This installation package was created to facilitate the use of the Wavelet Prosody Toolkit across different operating systems and research environments. Special thanks to the toolkit developers and the Python scientific computing community.

<!-- --- -->
<!--
*Installation package created: January 2026*
-->
